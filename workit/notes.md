# Workit Notes

Live: acli jira auth login --site "wavecomp.atlassian.net" --email "kmills@mips.com" --token < ~/.config/acli.token
Sandbox: acli jira auth login --site "mips-sandbox.atlassian.net" --email "kmills@mips.com" --token < ~/.config/acli.token
